import { createTheme } from "@mui/material/styles";

const theme = createTheme({

    palette: {

        primary: {
            main: "#22ffde"
        },

        secondary: {
            main: "#263832"
        }
    },

    typography: {

        fontFamily:
            "Roboto, sans-serif"
    }
});

export default theme;